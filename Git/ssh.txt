Generating SSH Keys
Step 1:
https://docs.github.com/en/github/authenticating-to-github/connecting-to-github-with-ssh/generating-a-new-ssh-key-and-adding-it-to-the-ssh-agent
https://support.atlassian.com/bitbucket-cloud/docs/set-up-an-ssh-key/
New way*
$ ssh-keygen -t ed25519 -C "your_email@example.com" 
$ ssh-keygen -t ed25519 -C "name.lastname@example.com" -f "<name_your_filename>"



Old way*
$ ssh-keygen -t rsa -b 4096 -C "firstname.lastname@company.net"
$ ssh-keygen -t rsa -b 4096 -C "name.lastname@example.com" 

Host git-codecommit.*.amazonaws.com
  User XXXXXXXXXXXXXXXXXXXXX
  IdentityFile ~/.ssh/id_rsa
Step 2:
Dealing with multiple keys 
https://stackoverflow.com/questions/21160774/github-error-key-already-in-use
id_rsa
cat ~/.ssh/config
vi ~/.ssh/config
        # Default GitHub
        Host github-test
        HostName github.com
        User git
        IdentityFile ~/.ssh/id_rsa

        Host github-public
        HostName github.com
        User git
        IdentityFile ~/.ssh/id_rsa_public

        Host <your_alias>
        HostName <server_url_or_ip_address>
        User <server_username>
        IdentityFile <ssh_private_key_location>
        ServerAliveInterval 60
        ServerAliveCountMax 120

        Host jm****no-bitbucket__companyname
        HostName bitbucket.companyname.com
        User jm****no
        PreferredAuthentications publickey
        IdentityFile /Users/mariano/.ssh/jm****no-bitbucket__companyname
        UseKeychain yes
        AddKeysToAgent yes

        Host <your_alias>
        HostName bitbucket.org
        User <server_username>
        PreferredAuthentications publickey
        IdentityFile ~/.ssh/id_ed25519_<alias>
        UseKeychain yes
        AddKeysToAgent yes

Step 3:
you will need to alter your .git/config remote url of your local repository to point to:
git@<SSH Host>:<git@github.com/etc...>.git 
git clone git@git.wpengine.com:production/<sitename>dev.git 
git clone git@bitbucket.org:production/<sitename>dev.git 
git@github-j*******o:C****T/<whatever>.git
git@<host>:<gitpath/companyname>/Apollo-graphql.git
  - cat ~/.ssh/config - to get the host git user

https://github.com/webupps/bootcampfront.git


//Bitbucket
git clone git@server_username:<companyname>/<repo>.git

//AWS codecommit
git clone git@server_username:ssh://git-codecommit.us-east-<number>.amazonaws.com/v1/repos/<reponame>
GIT_SSH_COMMAND="ssh -i ~/.ssh/<IdentityFile>" git pull origin master
git config --local core.sshCommand "ssh -i ~/.ssh/<IdentityFile>" 
or ssh-add ~/.ssh/id_rsa
https://superuser.com/questions/232373/how-to-tell-git-which-private-key-to-use
https://www.youtube.com/watch?v=RVia18FNHYo

Step 4:
ls ~/.ssh/*.pub 
ls ~/.ssh/config
cat ~/.ssh/id_rsa.pub
pbcopy < ~/.ssh/id_rsa.pub
paste the token to github





